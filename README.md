# Sample App for Jenkins + AccuKnox SAST

This is a simple Python application used for testing SAST scanning in Jenkins.

## Features
- Basic input handling
- Command execution (intentionally vulnerable for testing)

## Usage
```bash
python app.py
